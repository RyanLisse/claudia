# Claudia – Environment Variables Example for Neon + ElectricSQL
# Copy this file to `.env` or `.env.local` and replace the placeholder values.
# DO NOT commit real secrets to version control!

# ------------------------
# PostgreSQL (Neon)
# ------------------------
# Complete connection string generated by Neon’s dashboard.
DATABASE_URL=postgres://<username>:<password>@<host>/<database>?sslmode=require

# ------------------------
# ElectricSQL Cloud / Self-hosted
# ------------------------
# Base URL of your Electric sync service (no trailing slash)
ELECTRIC_API_URL=https://<your-electric-subdomain>.electric-sql.com

# ID that identifies this application’s replication source.  
# Safe to expose on the client.
ELECTRIC_SOURCE_ID=<source-id>

# Source secret used by your backend to sign requests to Electric.  
# NEVER expose this in client code.
ELECTRIC_SOURCE_SECRET=<source-secret>

# Optional: a human-readable identifier used in dashboards/metrics.
ELECTRIC_APP_NAME=claudia